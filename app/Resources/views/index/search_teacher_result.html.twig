{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    {{ form_widget(form)}}
    {{ form_end(form)}}

    {% for result in result %} 
        <a href="{{ path('teacher_profile', {'id': result.id}) }}">
        {{ result.name }}
        {{ result.surname }}
        {{ result.email}}
        </a>
    {% endfor %} 
    
{% endblock %}


